check the IP adress of the bridge
# bigBoy: 10.0.1.1
# vw1: 10.0.1.2
# vw2: 10.0.1.3

make excecutable:
Create a file: /etc/rc.local

File contents:

#!/bin/sh -e
sh /home/(path to script)/startup/startup.sh [ending IP]
exit 0
Save the file and make it executable with this command:

sudo chmod +x /etc/rc.local
